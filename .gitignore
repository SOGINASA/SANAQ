# Dependencies and build artifacts
node_modules/
build/
dist/
coverage/

# Environment and local configuration
.env
.env.*
!.env.example
!.env.production.example

# Editors and operating systems
.DS_Store
.idea/
.vscode/
*.log
# Trained checkpoints and evaluation reports are versioned. Generated training
# datasets stay local because they are reproducible and too large for regular Git.
backend/ml/artifacts/*.jsonl
backend/ml/artifacts/*.jsonl.gz

# Temporary artifacts
tmp/
*.tmp

# Local Docker state and real production secrets
postgres-data/
!.env.production.example
