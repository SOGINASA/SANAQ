# Keep these relative: CRA proxies them to Flask in development, while Nginx
# proxies them to the backend container in production.
REACT_APP_API_URL=/api/v1
REACT_APP_AI_STREAM_URL=/api/v1
REACT_APP_DEFAULT_LOCALE=ru
REACT_APP_ENABLE_MOCKS=false
